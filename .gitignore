env/
*.pyc
__pycache__/
instance/
todo.db
.DS_Store

#env/ → ignore your Python virtual environment folder

#*.pyc → ignore Python compiled cache files

#__pycache__/ → ignore Python cache directories

#instance/ → ignore Flask’s instance folder (where your SQLite DB lives)

#todo.db → ignore the database file directly, if it accidentally gets created outside

#.DS_Store → ignore Mac system files (if any)